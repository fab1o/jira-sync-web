//sync/step3
extends /layout

block css
  link(rel="stylesheet" type="text/css" href="/stylesheets/sync.css")

block content
  .jumbotron.white
    .container-fluid
      ul.nav.nav-tabs.strong(role="tablist")
        li.active(role="presentation")
          a(href="#sync" aria-controls="home" role="tab" data-toggle="tab") Sync
        li(role="presentation")
          a(href="#mapping" aria-controls="profile" role="tab" data-toggle="tab") Mapping

      .tab-content
        #sync.tab-pane.active(role="tabpanel")
          include ./sync/index.jade

        #mapping.tab-pane(role="tabpanel")
          include ./mapping.jade


block scripts
  script.
    var keyTo = "#{keyTo}";
    var keyFrom = "#{keyFrom}";

  script(src="/javascripts/sync.js")
